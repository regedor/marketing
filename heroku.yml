build:
  docker:
    web: Dockerfile  # This specifies that Heroku should use the Dockerfile for building the `web` process

run:
  web: /rails/run.sh  # This specifies that Heroku should use `run.sh` as the startup command